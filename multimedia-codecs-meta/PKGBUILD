# Maintainer: Dankcuddlybear <dankcuddlybear@proton.me>
pkgname=multimewdia-codecs-meta
pkgver=$(date +%Y.%-m.%-d)
pkgrel=$(expr $(date +%-S) + $(expr 60 \* $(date +%-M)) + $(expr 3600 \* $(date +%-H)))
pkgdesc='Video/audio codecs meta-package'
arch=(any)
url='https://github.com/dankcuddlybear/pkgbuilds'
depends=(
	gst-libav
	gst-plugins-bad
	gst-plugins-base
	gst-plugins-good
	gst-plugins-ugly
)
